apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: cloud-hw2
  region: us-east-1

nodeGroups:
  - name: workers
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
